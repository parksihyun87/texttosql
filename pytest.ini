[pytest]
# 테스트 디렉토리
testpaths = tests/pytest

# 테스트 파일 패턴
python_files = test_*.py

# 테스트 함수 패턴
python_functions = test_*

# 마커 정의
markers =
    slow: 느린 테스트 (LLM API 호출 등)
    integration: 통합 테스트 (DB 연결 필요)

# 출력 설정
addopts = -v --tb=short

# 경고 필터
filterwarnings =
    ignore::DeprecationWarning
